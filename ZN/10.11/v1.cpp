#include <iostream>
#include <vector>
using namespace std;
int main(){


    // int n; cin >> n; 
    // vector<int> v;
    // v.push_back(10);
    // cout << v[0] << endl;
    // v[0] = 2;
    // cout  << v[0];
    // vector<int> v2(n);


    // for(int i = 0; i < n;++i){
    //     int x; cin>> x;
    //     v.push_back(x);
    // }

    // for(int i = 0; i < v2.size(); ++i){
    //     cin >> v2[i];
    // }

    // for(int i = 0; i < v.size(); ++i){
    //     cout << v[i] << " ";
    // }
    vector <char> c;
    vector <char> c1(5, '+');
    for (int i = 0; i < c1.size(); ++i){}
    for (auto &i : c1){
        cout << i << ' ';
    }
    vector <char> :: iterator it;
}